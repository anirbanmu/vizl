/** @type {import('prettier').Config} */
const config = {
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 120,
  proseWrap: 'always',
  arrowParens: 'avoid',
  endOfLine: 'lf',
};

export default config;
